

#公共配置与profiles选择无关
mybatis:
  typeAliasesPackage: com.hoperun.entity
  mapperLocations: classpath:mapper/*.xml


server:
  port: 8008
  
# 注册中心配置
eureka:
  instance:
      hostname: localhost
      port: 8761
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/


spring:
  profiles: dev
  application:
    name: subject-manage
  datasource:
    url: jdbc:mysql://49.4.65.137:3306/subject?useUnicode=true&amp;characterEncoding=utf8
    username: root
    password: hoperun
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource